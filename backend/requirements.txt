fastapi>=0.111.0
uvicorn[standard]>=0.30.1
pydantic>=2.10.0
pydantic-settings>=2.4.0
sqlalchemy>=2.0.31
psycopg2-binary>=2.9.10
celery>=5.4.0
redis>=5.0.7
httpx>=0.27.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9
gunicorn>=26.0.0
pytest>=8.2.2
alembic>=1.13.1
email-validator>=2.0.0

# Live market data / mutual fund scraping - directly imported throughout
# app/services (market_data.py, tefas.py, portfolio.py signal fetching, etc.)
# but previously missing from this file, so a fresh "pip install -r
# requirements.txt" on a clean machine would fail with
# "ModuleNotFoundError: No module named 'borsapy'". Versions pinned to match
# what's actually installed and verified working in this project's venv.
borsapy==0.10.2
pytefas==0.4.1
pandas==3.0.3
bcrypt==5.0.0
slowapi==0.1.9
pyotp==2.9.0
qrcode[pil]==8.0
pywebpush==2.4.0
reportlab==5.0.0
sentry-sdk[fastapi]==2.19.2
# Not used for actual template rendering (this is an API-only backend) -
# sentry-sdk's StarletteIntegration (a dependency of FastApiIntegration,
# auto-enabled regardless of what's passed to integrations=[...]) does an
# unconditional `from starlette.templating import Jinja2Templates` during
# setup, which raises ImportError at sentry_sdk.init() time if jinja2 isn't
# installed - confirmed live, this crashed the app on startup the moment
# SENTRY_DSN got set. jinja2 itself is pure-Python with no heavy deps.
jinja2==3.1.5
